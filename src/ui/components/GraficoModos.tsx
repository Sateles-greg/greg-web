// import { Chart as ChartJS, Tooltip } from 'chart.js';
// Remover Bar e membros não existentes

import styles from './GraficoModos.module.css';

// ChartJS.register(Tooltip); // Apenas membros existentes

export default function GraficoModos() {
  // Substitua por dados mock ou lógica real


  return (
    <div className={styles.graficoContainer}>
      <h2>Uso dos Modos (últimos 7 dias)</h2>
      {/* Substitua por componente de gráfico real ou placeholder */}
      <div>Gráfico não implementado</div>
    </div>
  );
}
